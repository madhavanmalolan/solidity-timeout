{
  "name": "timeout-graph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ timeout-graph",
    "create-local": "graph create --node http://localhost:8020/ timeout-graph",
    "remove-local": "graph remove --node http://localhost:8020/ timeout-graph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 timeout-graph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.3",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
